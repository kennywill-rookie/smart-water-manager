# LS PLC (XGT/XGB) Programming Guide & Rules

이 문서는 LS Electric의 XGT(XGK, XGI, XGR) 및 XGB 시리즈 PLC 프로그래밍을 위한 표준 규칙과 주의사항을 정리한 가이드라인입니다. 지멘스(Siemens) 등 타사 기종과의 차이점을 중심으로 실무적인 코딩 규칙을 제시합니다.

---

## 1. 기종 선택 및 아키텍처 이해 (XGK vs XGI)

가장 먼저 본인이 사용하는 CPU가 **XGK**인지 **XGI**인지 파악해야 합니다.

| 구분 | XGK 시리즈 (Classic) | XGI/XGR 시리즈 (IEC Standard) |
| :--- | :--- | :--- |
| **언어** | 전통적인 래더(Ladder) 중심 | IEC 61131-3 (LD, ST, SFC, FBD) |
| **주소 체계** | 디바이스 주소 (P, M, K, D, Z 등) | 변수 중심 (Direct Variable: %I, %Q, %M) |
| **지멘스와의 유사성** | 미쓰비시(MELSEC)와 유사 | 지멘스(S7-1200/1500)와 매우 유사 |
| **데이터 처리** | 워드(Word) 단위 비트 연산 위주 | 데이터 타입(BOOL, INT, REAL) 엄격 적용 |

---

## 2. LS PLC만의 주소 및 메모리 규칙 (XGK 기준)

LS XGK 프로그래밍 시 가장 혼동하기 쉬운 디바이스 할당 규칙입니다.

### 2.1 주요 디바이스 영역
- **P (I/O)**: 입출력 카드와 직접 매핑. (P000~PFFF)
- **M (Internal)**: 내부 비트 메모리. 휘발성.
- **K (Keep)**: 정전 유지(Retentive) 비트 메모리. 배터리나 플래시 메모리로 보존됨.
- **L (Link)**: 통신(Pnet, Fnet 등) 공유용 영역.
- **F (Special)**: 시스템 상태 비트 (예: F0099 항상 ON, F0094 1초 클럭).
- **D (Data)**: 데이터 저장용 16비트 레지스터.
- **Z (Index)**: 인덱스 수정용 레지스터. 루프나 배열 처리 시 필수.

### 2.2 주의사항: 영역 중첩 (Overlap)
- LS는 미쓰비시와 달리 특정 영역의 비트 주소와 워드 주소가 명확히 구분되어 있지만, **D 영역의 비트 접근** 시 주의가 필요합니다. 
- 예: `D0000.0`은 D0000 워드의 0번 비트를 의미합니다. 하지만 가급적 비트 제어는 M이나 K를 사용하고 D는 수치 데이터용으로 분리하십시오.

---

## 3. 프로그래밍 명명 규칙 (Naming Conventions)

가독성을 높이기 위해 전역 변수와 로컬 변수에 접두어(Prefix)를 사용합니다.

### 3.1 변수 접두어 규칙
- `b` (BOOL): 예) `b_Motor_Run`
- `n` (INT/WORD): 예) `n_Error_Code`
- `f` (REAL/FLOAT): 예) `f_Water_Level`
- `t` (TIME/TIMER): 예) `t_Delay_1s`
- `s` (STRING): 예) `s_Barcode_Data`

### 3.2 디바이스 기반 명명 (XGK)
- 물리적 I/O는 실제 주소를 포함하는 것이 좋습니다.
- 예: `IN_P0000_Start_PB`, `OUT_P0020_Main_MC`

---

## 4. 로직 작성 규칙 및 주의사항

### 4.1 스캔 타임(Scan Time) 관리
- LS PLC는 지멘스에 비해 CPU 성능이 우수하지만, 무분별한 FOR-NEXT 문 사용은 Watchdog Error를 유발합니다.
- 대량의 데이터 연산은 **타스크(Task) 프로그램**으로 분리하여 주기적으로 실행하십시오.

### 4.2 엣지(Edge) 신호 사용
- **|P| (Positive)**, **|N| (Negative)** 접점을 적극 활용하십시오.
- 지멘스의 `R_TRIG`, `F_TRIG` 블록보다 래더 내 엣지 접점이 훨씬 직관적이고 연산 속도가 빠릅니다.

### 4.3 이중 코일(Double Coil) 금지
- 한 프로그램 내에서 동일한 출력 주소를 두 번 이상 사용하지 마십시오.
- 복잡한 조건은 내부 메모리(M)를 병렬로 모은 뒤, 최종 출력 단에서 하나로 합치십시오.

---

## 5. 지멘스 유저가 실수하기 쉬운 LS의 특징

1. **데이터 타입 변환**: XGK에서는 `INT`와 `DINT` 간의 연산 시 반드시 타입 변환 명령어(`I2D` 등)를 명시해야 합니다. (자동 형변환 미지원)
2. **타이머 동작**: 
   - LS의 타이머(T)는 접점이 살아야 카운트가 시작되며, 리셋 시 즉시 초기화됩니다. 
   - 지멘스의 IEC 타이머(DB 기반)와 달리 **T 영역 번호(T0~T2047)**를 직접 관리해야 하므로 번호 중복에 주의하십시오.
3. **비교 연산자**: 
   - 래더에서 비교 연산 시 `[ =  D0  100 ]` 형태로 사용하며, 이는 지멘스의 비교 블록과 유사하지만 입력 위치가 다릅니다.

---

## 6. 통신 및 특수 모듈 (Communication)

- **Cnet/Enet**: LS는 `XG-PD`라는 별도의 네트워크 설정 툴을 사용합니다. 코드 내에서 통신 로직을 짜기보다 `XG-PD`에서 파라미터 설정을 통해 데이터를 주고받는 방식(P2P)이 훨씬 안정적입니다.
- **Modbus**: LS는 Modbus RTU/TCP를 기본 지원하며, 고정 주소 매핑 테이블을 확인하십시오. (예: %MW0 -> Modbus Address 40001)

---

## 7. 유지보수를 위한 팁

1. **설명문(Comment)의 생활화**: 
   - 변수 설명문뿐만 아니라 **네트워크(Rung) 설명문**을 반드시 작성하십시오.
2. **프로그램 분할**: 
   - 한 파일에 모든 코드를 넣지 말고 `Main`, `Alarm`, `Manual`, `Auto`, `Comm` 등으로 프로그램을 나누어 관리하십시오.
3. **심볼(Symbol) 활용**: 
   - 주소(P00)를 직접 사용하기보다 의미 있는 심볼 명칭을 먼저 정의하고 코딩하십시오.
